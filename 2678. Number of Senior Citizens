class Solution {
public:
    int countSeniors(vector<string>& details) {
        int ans=0;
        int temp=0;
        for(auto it:details){
             temp=(it[11]-'0')*10 + (it[12]-'0');
            cout<<temp;
            if(temp>60){
            ans++;
            }
        }
        return ans;
    }
};
